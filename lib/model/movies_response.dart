class Movie {
  String title;
  int userRating;
  int grade;
  int reservationGrade;
  String id;
  String date;
  String thumb;
  double reservationRate;

  Movie(this.title, this.userRating, this.grade, this.reservationGrade, this.id,
      this.date, this.thumb, this.reservationRate);
}
